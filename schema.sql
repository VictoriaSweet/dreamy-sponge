-- DROP DATABASE
DROP DATABASE IF EXISTS ecommerce_db;

-- CREATE DATABASE
CREATE DATABASE ecommerce_db;

CREATE TABLE category [if not exists] 'category' (id INT PRIMARY KEY, name VARCHAR(20));
INSERT INTO category VALUES (varchar);
INSERT INTO category VALUES ();
-- -- Category
-- id
-- Integer
-- Doesn't allow null values
-- Set as primary key
-- Uses auto increment
-- category_name
-- String
-- Doesn't allow null values